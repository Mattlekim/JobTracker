<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="UiInterface.Layouts.BankAccounts"
             Title="Bank Accounts">

    <ContentPage.Resources>
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="StrokeThickness" Value="0" />
            <Setter Property="StrokeShape" Value="RoundRectangle 10" />
            <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=#F2F4F7, Dark=#1E1E1E}" />
            <Setter Property="Padding" Value="10" />
        </Style>
        <Style x:Key="RowButton" TargetType="Button">
            <Setter Property="BackgroundColor" Value="Transparent" />
            <Setter Property="BorderWidth" Value="2" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="12,4" />
            <Setter Property="FontSize" Value="13" />
        </Style>
    </ContentPage.Resources>

    <ScrollView>
        <VerticalStackLayout Padding="12" Spacing="10">
            <Label Text="Each account remembers its own statement layout, and the statements and expenses imported from it are tracked against it. With more than one account, importing a statement asks which it is from - and checks the file against what that account's statements usually look like."
                   FontSize="12" TextColor="Grey"></Label>

            <VerticalStackLayout x:Name="vsl_accounts" Spacing="8"></VerticalStackLayout>

            <Button Text="Add Account" BackgroundColor="Transparent" TextColor="Green" BorderColor="Green" BorderWidth="2" Clicked="bnt_add_Clicked"></Button>

            <Label Text="An account cannot be deleted - what was imported from it stays tracked against it for as long as the records are kept. Archive an account that is closed or no longer imported from, and it stops being offered; unarchive it any time."
                   FontSize="12" TextColor="Grey"></Label>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
